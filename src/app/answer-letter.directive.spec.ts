import { AnswerLetterDirective } from './answer-letter.directive';

describe('AnswerLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerLetterDirective();
    expect(directive).toBeTruthy();
  });
});
